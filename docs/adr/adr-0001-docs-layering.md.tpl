---
title: "ADR 0001 · 文档分层规则"
type: design
status: active
date: {{DATE}}
description: |
  规定 {{PROJECT_NAME}} 使用顶层入口 + phase / changes / adr / research + 关注域子目录的文档分层结构。
---

# ADR 0001 · 文档分层规则

## Context

项目一开始如果只有少量文档，平铺结构很直观；但随着产品设计、技术设计、阶段规格、局部优化计划和长期决策逐步增加，所有文档都放在 `docs/` 顶层会带来几个问题：

1. 默认入口过长，AI 和团队成员不容易快速判断先读哪份。
2. 主线规格、局部变更、长期决策混在一起，职责边界模糊。
3. 前端或工程类文档一多，就会稀释顶层导航的注意力。

## Decision

采用以下分层结构：

1. 顶层只保留默认入口必须知道的系统级文档。
2. 主线里程碑进入 `phase/`。
3. 非主线但值得追踪的局部演化进入 `changes/`。
4. 已稳定的长期决策进入 `adr/`。
5. 调研和背景材料进入 `research/`。
6. 当某类文档只服务于一个明确关注域并逐渐增多时，收纳到二级目录，通过 `README.md` 提供二级入口，例如 `frontend/` 与 `engineering/`。

## Consequences

正面影响：

1. 顶层导航更短，默认阅读路径更清晰。
2. 文档修改时更容易判断应改哪一层。
3. 聊天中形成的重要结论更容易沉淀为 change plan 或 ADR。

代价：

1. 需要额外维护目录 README 和交叉引用。
2. 新增文档前，需要先判断它属于哪一层，而不是直接平铺新增。

## Alternatives Considered

### 方案 A：所有文档都平铺在 docs/

优点是简单，但随着文档增长，入口噪音会迅速增加。

### 方案 B：所有改动都写进 phase/

优点是结构少，但会让 Phase 文档膨胀，并把局部优化和主线里程碑混在一起。

### 方案 C：只写 README，不建 ADR / changes

优点是成本低，但长期规则和局部演化都缺少稳定承载层。